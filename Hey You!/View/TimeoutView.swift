//
//  TimeoutView.swift
//  Hey You!
//
//  Created by Brendan on 2021-03-24.
//

import SwiftUI

struct TimeoutView: View {
    var body: some View {
        VStack(spacing: 24) {
            Text("We couldn't find your device :(")

            Text("Make sure it's turned on, and if all else fails try completely closing and then restarting the app.")
        }
    }
}
